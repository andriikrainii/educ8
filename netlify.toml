[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# Route /api/state -> the "state" function
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
